plan/

# Build artifacts
/fluxa
/bin/
/dist/

# Runtime data — the SQLite store and its WAL/SHM sidecars are produced
# every time the gateway boots, so they must never end up in commits.
*.db
*.db-journal
*.db-wal
*.db-shm
fluxa.yaml

# Editor
.vscode/
.idea/
.DS_Store

# local go caches
.gocache/
.gomodcache/
.gopath/
